function reducer(state, action) {
  switch (action.type) {
    case 'TOGGLE_ACCOUNT_MODAL': {
      // Do something with action
    }
    case 'TOGGLER': {
      // Do something with action
    }
    default: return state
  }
}